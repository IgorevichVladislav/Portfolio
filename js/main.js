$(function() {

    var mixer = mixitup('.work__list', {
        animation: {
            enable: true,
            effects: 'scale fade',
            duration: 700,
            easing: 'cubic-bezier(0.645, 0.045, 0.355, 1)',
            
           
        }
    });

});